'''
Created on 2011/5/6

@author: korprulu
'''
from django.conf.urls.defaults import patterns
from mobile.cstp.main import cstp, introduction, features, courses, rules,\
    admission

urlpatterns = patterns('',
                       (r'^/?$', cstp),
                       (r'^introduction/?$', introduction),
                       (r'^features/?$', features),
                       (r'^courses/?$', courses),
                       (r'^rules/?$', rules),
                       (r'^admission/?$', admission),
                       )