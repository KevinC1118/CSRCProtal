from django.conf.urls.defaults import patterns


urlpatterns = patterns(
   'file.youtube.views', 
   (r'^upload/?$', 'upload'), 
   (r'^upload/result/?$', 'uploadResult'), 
   (r'^delete/?$', 'delete'), 
)