'''
Created on 2011/5/5

@author: korprulu
'''
from django.conf.urls.defaults import patterns
from cstp.main import cstp, introduction, features, courses, applyProcess,\
    admission, download

urlpatterns = patterns('',
                       (r'^/?$', cstp),
                       (r'^introduction/?$', introduction),
                       (r'^features/?$', features),
                       (r'^courses/?$', courses),
                       (r'^applyprocess/?$', applyProcess),
                       (r'^admission/?$', admission),
                       (r'^download/?$',download)
                       )